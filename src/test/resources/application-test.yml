spring:
  application:
    name: e4s-index-system-test

  data:
    redis:
      host: localhost
      port: 6379

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

index:
  cache:
    max-size: 100

logging:
  level:
    com.e4s.index: DEBUG
